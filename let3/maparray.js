const finalParticipants = ["Taylor", "Donald", "Don", "Natasha", "Bobby"];

const announcemts = finalParticipants.map((member) => {
  return member + " joined the contest.";
});

console.log(announcemts);
