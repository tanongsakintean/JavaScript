const message = "Hello";
const place = "World!";

//logging out string
console.log("Hello World!");

//using substitutions
console.log("Hello, %s", place);

// using a string literal
console.log(`${message}, ${place}`);
